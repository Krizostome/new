import { PaginationSlicePipe } from './pagination-slice.pipe';

describe('PaginationSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new PaginationSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
